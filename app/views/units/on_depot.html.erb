<% title 'Listing depot' %>

<%= render 'layouts/search' %>
<%= render 'list' %>

<div class="actions">
  <% if can?(:manage, Unit) %>
    <%= link_to 'New Unit', new_unit_path(:on_depot => true) %>
  <% end %>
</div>
