<% title 'Listing units' %>

<%= render 'layouts/search' %>
<%= render 'list' %>

<div class="actions">
  <% if can?(:manage, Unit) %>
    <%= link_to 'New Unit', new_unit_path %>
  <% end %>

  <%= link_to 'Export all as CSV', units_path(:format => :csv) %>
</div>
